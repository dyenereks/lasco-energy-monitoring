# Secrets / local config (contains device keys + API key)
config.json

# SQLite history database (+ WAL/SHM sidecar files)
energy.db
energy.db-wal
energy.db-shm

# Runtime logs & local backup
logger.log
*.log
backup-log.jsonl

# Node
node_modules/
npm-debug.log*

# OS junk
.DS_Store
